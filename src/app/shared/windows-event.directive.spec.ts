import { WindowsEventDirective } from './windows-event.directive';

describe('WindowsEventDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowsEventDirective();
    expect(directive).toBeTruthy();
  });
});
