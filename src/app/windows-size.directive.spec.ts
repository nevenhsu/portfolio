import { WindowsSizeDirective } from './windows-size.directive';

describe('WindowsSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowsSizeDirective();
    expect(directive).toBeTruthy();
  });
});
